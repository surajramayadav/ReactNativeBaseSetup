import React from "react";
import { Text } from "react-native";

function TextError({ error,name }) {
  // const {errorValue }=errors
console.log(error)

  // return <Text style={{color:'red'}}>{errors.name }</Text>;
}

export default TextError;
